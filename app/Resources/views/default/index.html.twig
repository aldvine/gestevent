{% extends 'base.html.twig' %}

{% block body %}
   <div class=" container">
    <div class="mx-auto">
      <h1> {{'welcome'|trans}}</h1>
      <h3>{{'homepage.explication'|trans}}</h3>
      <h4>{{'homepage.choix'|trans}}</h4>
      <div class="text-center  ">
        <a class="btn btn-primary btn-lg btn-block" href="{{ path('event_index') }}">{{'events'|trans}}</a>
      </div>
      <br>
      <div class="text-center  mx-auto">
        <a class="btn btn-warning btn-lg btn-block  " href="{{ path('inscription_index') }}">{{'myinscriptions'|trans}}</a>
      </div>
    </div>
   </div>
            
{% endblock %}


